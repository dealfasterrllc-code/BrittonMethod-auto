# Python runtime / constraints
# NOTE: use Python 3.11 recommended in runtime on Render / Replit / Docker

# Core runtime
requests>=2.28
beautifulsoup4>=4.11
pandas>=1.5
numpy>=1.23
sqlalchemy>=1.4
python-dotenv>=1.0

# Web / dashboard / app
flask>=2.2
streamlit>=1.20
gunicorn>=20.1          # production WSGI for Flask
uvicorn>=0.22.0         # ASGI server (useful for async endpoints or FastAPI later)

# APIs & comms
twilio>=8.0
openai>=0.27
tavily-python>=0.3.5    # Tavily Python SDK (real-time web/searching)
boto3>=1.26             # optional - needed if using S3 for LOIs / evidence

# ML / plotting / modeling
matplotlib>=3.6
scikit-learn>=1.2
xgboost>=1.7            # optional for stronger ranking models
joblib>=1.3             # parallel utilities for ML

# Document / artifacts
python-docx>=0.8.11
openpyxl>=3.1.2         # create/read xlsx
xlsxwriter>=3.1.2       # excel writer options
reportlab>=4.0          # PDF generation (if needed)
Pillow>=9.5             # image processing for rehab/vision tasks
pdfminer.six>=20221105  # optional: PDF parsing

# DB & migrations
psycopg2-binary>=2.9.6  # Postgres driver
alembic>=1.11           # DB migrations
sqlalchemy-utils>=0.41.0

# Async / HTTP clients
httpx>=0.24
aiohttp>=3.8

# Scraping / headless browser (for dynamic JS sites)
playwright>=1.33.0      # recommended for modern JS scraping (requires post-install step)
# NOTE: Playwright needs `playwright install` during build/deploy

# Search & indexing
elasticsearch>=8.8.0    # or opensearch-py if you use OpenSearch

# Queue / background jobs
redis>=4.5.5
rq>=1.1.0               # simple queue (or replace with celery)
celery[redis]>=5.3.2    # optional: advanced task queue (install redis server)

# Geospatial / address tools
geopy>=2.3.0
usaddress>=0.5.10       # US address parsing
shapely>=2.0.1
geoalchemy2>=0.11.3     # if using PostGIS

# Optional / convenience & validation
pydantic>=1.10
s3fs>=2023.11.1         # access S3 via pandas if desired
python-magic>=0.4.27    # file type detection

# Security / auth / crypto
cryptography>=41.0
python-jose>=3.3.0      # JWT handling

# Observability / error tracking
sentry-sdk>=1.28.0
prometheus-client>=0.16.0
opentelemetry-api>=1.14.0
opentelemetry-sdk>=1.14.0

# Testing & CI
pytest>=7.0
pytest-cov>=4.0
coverage>=6.0
flake8>=6.0

# Dev / tooling
black>=24.3
isort>=6.0

# Optional: email / forms / upload parsing
python-multipart>=0.0.6

# Optional: for advanced scraping of some sites (only if you need it)
selenium>=4.10.0
undetected-chromedriver>=3.4.0

# Optional heavy CV/ML (only if you plan local model inference)
# torch>=2.2.0+cpu          # enable if you need torch models locally (large download)

# NOTE: If targeting smaller Docker images, trim optional libs such as selenium, torch, xgboost.

# NOTE: If targeting smaller Docker images, trim optional libs.
